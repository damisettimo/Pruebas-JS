let stockProductos = [];


/*class Producto {
    constructor ( id, nombre, tipo, cantidad, desc, precio, contenido, img ){
        this.id = id;
        this.nombre = nombre;
        this. tipo = tipo
        this.cantidad = cantidad
        this.desc = desc
        this.precio = precio
        this.contenido = contenido
        this.img = img
    }

}



 const cargar = async () => {
   const response = await fetch("./stock.json")
   const datos = await response.json();
    try {
        datos.forEach(dato => {
        stockProductos.push(new Producto(producto.id, producto.nombre, producto.tipo, producto.cantidad, producto.desc, producto.precio, producto.contenido, producto.img));
    });
    }catch {
            console.log ("error")
        }
}
cargar ();*/
